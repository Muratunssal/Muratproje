✅ requirements.txt

---

scapy
flask
dnslib

---

Açıklamalar:
scapy: Ağ paketlerini oluşturmak, dinlemek ve manipüle etmek için kullanılır (ARP, DNS, DHCP vs.).

flask: Sahte web sunucusu (phishing/simulation) arayüzü için.

dnslib: Gelişmiş DNS proxy (örn. DNSChef benzeri yapı) oluşturmak için.

Kurulum Komutu:
Proje klasörüne gidip terminale şu komutu yazman yeterli:

bash
pip install -r requirements.txt
